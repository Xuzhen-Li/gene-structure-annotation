# 结构注释 · 验收勾选表（行不行？）

打印或复制到笔记里打勾。权威细则仍是英文 [`../EVALUATION.md`](../EVALUATION.md)。

中文讲解：[11_验收L0L1L2.md](11_验收L0L1L2.md)

---

## 先选目标档

- [ ] **L0** 档位（METHODS：`grade=L0` + 通常 `status=provisional`）— 证据薄 / 债未清，**不能**当论文终稿  
- [ ] **L1** 档位（`grade=L1` + `status=qualified`）— 默认实验室放行  
- [ ] **L2** 档位（`grade=L2` + `status=qualified` + overlay S5）— 不是另选「S5 支」

> **口播：** `grade` = L0/L1/L2（或 F-L*）；`status` 只写 `provisional` 或 `qualified`。**不要**写 `status=L1`。

你勾的是：________

---

## L1 硬门槛（任一 ✗ ⇒ 不能宣称 L1）

- [ ] **G1** 组装可注：`ASSEMBLY_OK` + Asm1（谱系 / N50 / 倍性说清）  
  - 别人组装的也能勾：METHODS 写来源与判断即可（不必亲自 hifiasm）  
- [ ] **G2** Soft-mask + **trusted** TE 库（无 hard-mask；无生 EDTA / 整份 working 当金库）  
  - 无现成 sha256：自己 `sha256sum` 写入 METHODS 即可  
- [ ] **G3** 草稿支写清（S1/S11/S11-lite/…）+ 工具**版本**（lift-only 无补洞 → 写 **S11-lite**，`status=provisional`）  
- [ ] **G4** GFF 过 gffread/AGAT；记下 gene / mRNA / CDS 数  
- [ ] **G5** `proteins.faa` 从**本轮**放行 GFF 重抽；isoform 政策写明  
- [ ] **G6** 蛋白 BUSCO：**Completeness + 谱系全名**（如 `viridiplantae_odb12`）  
  - 本仓**不设**全球 Completeness 数字门槛；百分比在 Asm1/S5 自定  
- [ ] **G7** PSAURON + 优先表；最差点已修 **或** METHODS 显式暂缓  
  - 优先表可用 `pipeline/02_priority_loci.py`  
- [ ] **G8** `release/<TAG>/` 有 GFF + 蛋白 + METHODS（无私有 BAM/FASTQ）  
- [ ] **G9**（若相关）串联 / 抗病 / QTL 窗口已审或列入 open items  

L1 结果：☐ 通过 ☐ 未通过（缺：________）

---

## L2 额外（只有冲论文/T2T 才勾）

- [ ] OMArk（或等价一致性检查）已跑并写入 METHODS  
- [ ] 第二轮策展 / GSAman 深度达到 S5 约定  
- [ ] 停手规则写清（避免无限抛光）

---

## 软指标 / 可选加强（写进 METHODS，不设全球阈值）

- [ ] Compleasm 与 BUSCO 对照（同源谱系家族）
- [ ] gffcompare（有参考或第二套草稿时）
- [ ] 有 RNA 时：外显子/内含子支持比例；可选 BRH
- [ ] 可选打包工具：GAQET2 / AnnoAudit / atol-qc…（仍要勾本表硬门槛）
- [ ] 宣称 L2 时：OMArk 表已进 `qc/`

来源架：[英文 `QUALITY_SOURCES.md`](../QUALITY_SOURCES.md)

## 自动不合格（出现任一条 ⇒ 任何档都不行）

- [ ] 没有 hard-mask 冒充 soft 喂 BRAKER/GALBA  
- [ ] 没有用生 EDTA / 整份 working 当 `--curatedlib`  
- [ ] 没有在有 RNA+蛋白时用 S13 **静默顶替** S1  
- [ ] 没有在本仓宣称已做 GO/KEGG（那是功能仓）  
- [ ] 没有把私有 reads 放进 git / 放行包  

卫生：☐ 全干净 ☐ 有问题（________）

---

## 一句话结论（写进 METHODS）

```text
Structure release <TAG>: grade=L0|L1|L2 ; status=provisional|qualified ; primary draft=S__|S11-lite ;
BUSCO=<C>% (<lineage>) ; TE lib=<trusted file + sha256> ;
open items: …
```

交给功能仓前：结构至少 **L1**（或诚实写 L0 且 FA 只能暂定）。

---

## 放行包冒烟（可选）

```bash
python3 pipeline/check_release_pack.py path/to/release/TAG
```

只检查文件在不在，**不能**代替上面的门禁勾选。

## 打印推荐验收命令

```bash
set -a && source config/local.env && set +a
python3 pipeline/print_qc_commands.py
```

只打印，不替你跑。

> 英文 Wiki：放行目录长什么样 → [Golden pack](https://github.com/Xuzhen-Li/gene-structure-annotation/wiki/Evaluate-release#golden-pack--what-done-looks-like)。
