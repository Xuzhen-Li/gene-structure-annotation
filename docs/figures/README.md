# Figures

| File | Role |
|------|------|
| `structure_overview.svg` | **Edit this** — beginner map (pipeline + branch pick + start here) |
| `structure_overview.png` | README / zh render (`rsvg-convert` from the SVG) |

Public labels stay English. Chinese teaching prose is under `docs/zh/`.
Sibling function overview lives in gene-function-annotation `docs/figures/`.
