# Audit 2026-09-14 — gene-structure-annotation

Scope: identity/layer split, front door, consistency, teaching spot-check, hygiene.  
Method: greps + Python relative-link check under `README.md` + `docs/**/*.md`.  
Policy: fix **clear bugs** only (broken links, wrong A6 / identity leftovers, index mismatches). No curriculum rewrite.

## Pass / Fail

| Area | Result | Notes |
|------|--------|-------|
| A. Identity / layer split | **Pass** | Primary product = GFF + proteins. A6 is hand-off only (`pipeline/A6_functional_optional.md`). No F1 runners. `TOOLS.md` “Functional tools” points to sibling (no F1 eggNOG table as this-repo product). |
| B. Front door / usability | **Pass** (after fixes) | Bilingual banner + 3-step path in README. `docs/START_HERE.md` → `zh/README.md`. zh curriculum 00–16 + 99 matches files on disk. Relative links: **0 broken** after fixes (was 15). |
| C. Consistency | **Pass-with-notes** | `flow.py` `choose_branch` matches ROADMAP priority (S11 → S14 → S3 → S1 → S2 → S13 → S6). zh/11 L0/L1/L2 matches English EVALUATION. BILINGUAL.md coherent. |
| D. Teaching quality | **Pass** | WHY pages exist (00, 14–16). Lessons 01–13 have `## 为什么`. Dedicated WHY pages use H1 “为什么…” (no separate `## 为什么` — intentional). No empty stubs / TODO placeholders found. |
| E. Hygiene | **Pass** | No accidental huge binaries. `config/example.env` is structure-scoped; FA keys deferred to sibling comment. |

**Overall grade: pass-with-notes**

## Findings (pre-fix)

### Broken relative links (15)

| File | Bad target | Cause |
|------|------------|-------|
| `docs/SCENARIOS.md` | `../tools/hrp.md` | Wrong path; HRP lives in FA repo |
| `docs/SCENARIOS.md` | `notes/ragnarok.md` | Missing note page |
| `docs/SCENARIOS.md` | `../tools/repeatmasker.md`, `../tools/pasa.md` | Should be `tools/…` from `docs/` |
| `docs/SCENARIOS.md` / `DETAILED_GUIDE.md` / `tools/evm.md` / `notes/copetti.md` / `steps/dclab/README.md` | `notes/cantulab_evm.md` | Missing; content lives under `steps/dclab/` |
| `docs/TOOLS.md` / `tools/hifiasm.md` | `tools/yahs.md` | Page never added |
| `docs/TOOLS.md` | `tools/hrp.md` | FA tool wrongly listed as local |
| `docs/cases/vitis-t2t-s1-s5.md` | `../pipeline/03_…`, `../pipeline/04_…` | Needs `../../pipeline/…` |

### Identity greps

No live claims of FA as this repo’s PRIMARY product / `FUNCTIONAL_MAIN` / F1 eggNOG-as-local-tool table. A6 explicitly denies `F1_diamond.sh` here.

### Consistency notes (not bugs)

- ROADMAP lists S6 as provisional thin-evidence draft; flow assigns S6 + L0 when no RNA/proteins — aligned.
- GPU compare with RNA becomes overlay `S13_compare` rather than replacing S1 — aligned with ROADMAP “alongside, not instead.”

## Fixes applied

1. Retargeted SCENARIOS / DETAILED_GUIDE / copetti / evm / dclab links from missing `cantulab_evm.md` → `steps/dclab/README.md` (or inline note).
2. Fixed SCENARIOS `tools/repeatmasker.md` and `tools/pasa.md` paths.
3. HRP links → FA repo `docs/tools/hrp.md` (GitHub).
4. RAGNAROK note link → `PEER_PIPELINES.md` §RAGNAROK.
5. YaHS links → upstream https://github.com/c-zhou/yahs (no local stub invented).
6. Vitis case pipeline links → `../../pipeline/03_evidence_checklist.md` and `04_gsaman_curation.md`.
7. Added this audit; linked from README docs table.

## Deferred

- No local `docs/tools/yahs.md` or `docs/notes/ragnarok.md` / `cantulab_evm.md` pages (links retargeted instead of inventing thin stubs).
- Dedicated WHY pages omit a secondary `## 为什么` heading (H1 already is the WHY); left as-is.
- No flow.py / ROADMAP logic rewrite (no mismatch requiring a code change).
