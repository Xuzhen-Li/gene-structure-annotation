# Plant sim — Vitis S1 + S7 (teaching)

Short classroom pack. Copy `answers.sim.yaml` (and peek `env.snippet` / `plan.md`).

- **S1 ≠ S7.** S1 = primary draft (BRAKER-class). S7 = tandem/disease-window **overlay** (G9), not a second primary.
- Soft-mask needs a **trusted** TE curatedlib (+ real sha256). Working / raw EDTA / `YOUR_SHA256` → forbid A0.
- **S7a** window prep can precede FA **F8** (NLR); the function sibling uses `want_nlr` — do not confuse S7 with F8.
- **Do not download genomes in classroom** — list paths / use instructor FASTA; tonight stop is plan + path list.

See also: `docs/zh/植物三种模拟.md`, Soft-mask Done when, `docs/TRUSTED_TE_PATH.md`.
